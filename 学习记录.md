# 学习记录

> 从 LEVEL0 环境检查到 LEVEL4 U-Net 手写擦除的逐级学习记录，按"思路 → 遇到的问题 → 解决 → 问题回答"整理。

## 目录

- [LEVEL0：环境检查](#level0环境检查)
- [LEVEL1：MLP 手写数字识别](#level1mlp-手写数字识别)
- [LEVEL2：CNN 图像分类](#level2cnn-图像分类)
- [LEVEL3](#level3)
  - [1. AlexNet](#1-alexnet)
  - [2. ResNet](#2-resnet)
- [LEVEL4：语义分割 U-Net](#level4语义分割-u-net)


---

## LEVEL0：环境检查

对系统环境进行检查，因为之前就已经存在环境，所以未存在问题。

### 遇到的问题

**1. LEVEL0 的代码最初为手写，但在 AI 检查时 AI 提到，测试 CPU 与 GPU 性能的代码缺少热身与同步阶段**

**解决：** 了解到测试需要预热来加载初始化环境，且 GPU 运算是异步执行，直接计算时间不准确。AI 加入这两个模块后，GPU 耗时普遍提升 30% 左右，侧面验证了这一观点。

---

## LEVEL1：MLP 手写数字识别

MLP 是**多层感知机**或**多层感知器**。它是最基础、最经典的一种前馈人工神经网络。它对图像数据的处理是完全展平，然后计算，所以只采用全连接层 + RELU + softmax。

### 思路

1. 准备数据集：从 data 提取数据，转成数据集，再封装成数据加载器
2. 定义模型：全连接 + relu 激活函数
3. 训练模型：保存参数与损失曲线图像
4. 读取训练得到的参数，进行测试

### 遇到的问题

**1. 绘图保存的第一次时出现找不到文件夹的情况**

**解决：** 加入一个文件夹审查机制，这样找不到系统会自动创建，从根本上避免了问题。

**2. 训练时有 99% 以上的准确度，但是测试时只有 97.75%，怀疑是否出现过拟合**

**解决：** 尝试加入 dropout，但结果影响不大。让 AI 检查，其多跑了几轮发现 loss 还在下降，所以初步判断这 1.8% 左右的差距是数据不泄露下难以避免的一部分。

### 问题回答

**1. 假设我们将 MLP 看成一个黑盒，只需要提供输入就可以得到一个输出作为我们的结果，那么我们对 MLP 的输入是什么样子的？图片是怎么被输入进 MLP 进行处理的？**

这里的输入是图片，然后用 ToTensor 直接转成张量，torch 中封装了很多对张量的计算，即可以对其进行处理。实际训练，要把张量变成数据集，然后再封装成数据加载器。

**2. 一个 MLP 在没有经过任何的训练的情况下输入数据后应该会输出完全错误的结果，那么在直觉上，MLP 是怎么通过训练让输出的结果越来越正确的？**

他的初始参数会随机设定，也可以通过一些之前的人设置好的 API 进行设定，但是我建的 MLP 层数较浅，所以省略了这一步。然后它会通过模型由输入值得到输出预测值，再通过损失函数算出损失，计算梯度，再对参数反向更新。

**3. 如果我们将神经网络看作一个输入 → 输出的映射，那么要怎么理解基于输入和输出（基于数据）逆向映射的这一个过程？和对输入做变换得到输出有什么区别？**

逆向映射就是由正向传播得到的损失函数，计算梯度，反向传播到权重更新处，通过设置学习率，对权重进行不断的自动更新迭代。相较于对输入做变换得到输出，这样由机器直接做更新，显然更快，但是权重数据均由机器尝试得到，没有数学公式支持，可解释性差很多。

---

## LEVEL2：CNN 图像分类

CNN：卷积神经网络，相较于 MLP 多加了卷积与池化层，但是其他结构大致相同。初步思路为将其他代码复制粘贴，只改模型部分，这里选择在前面加两个卷积层两个池化层。

### 遇到的问题

**1. CNN 较之于 MLP 仅有 1.1% 的提升，因为 MLP 的准确率已经很高了，这样无法体现出 CNN 的优越性**

**解决：** 使用 FashionMNIST 进行训练测试，发现 CNN 有 3% 左右的提升，更能展示其优越性。

**2. CNN 在使用 fashionmnist 时，最高只有 91% 准确率，且难以提升，后续存在过拟合问题**

**解决：** 加入深层结构、归一化、dropout，此即变成后续的 AlexNet 等。为了与 MLP 比较，此代码不再修改。

### CNN 的优势

图像作为像素点的空间拼接，相邻像素点本来就具有难以忽略的关系，但是 MLP 将其展平，无疑是直接将这些信息忽略；而 CNN 采用卷积核的方法，对相邻块的像素进行处理，考虑这些因素，可以提取更多有效的特征。并且卷积层与池化层本身就使得 CNN 的深度更深，所以效果更好。

---

## LEVEL3

### 1. AlexNet

相较于之前的 CNN，它加入了更深的网络，具备 dropout 和数据增强来防止过拟合，同时通过归一化避免梯度消失或者爆炸。

此外，它还采用动量法，对学习率做动态调整，这样可以有效避免学习率过早过量的降低。

**思路：** 依旧以前面的基础 CNN 为基础，然后做出以下改动。

1. **做数据增强：** 对图像随机平移反转，使得数据无限不重复，可以有效避免过拟合问题
2. **深层网络：** 相较于之前的 CNN，多加入三个卷积层，使其能提取更多特征
3. **在全连接层前加入 dropout：** 使神经元随机失活，可以避免神经元之间对固定特征形成依赖，也可以避免过拟合
4. **采用 SGD + 动量法：** 对学习率做动态调整，避免学习率过早过快的降低

#### 遇到的问题

**1. 数据训练过慢，一轮要 15s 左右，跑 90 轮要 20 多分钟**

**解决：** 加入三段代码：

- `torch.backends.cudnn.benchmark = True`
  本机使用英伟达显卡，所以可以使用这个让 cudnn 预计最快的卷积计算法并运用
- `NUM_WORKERS = 8`
  允许多线程的使用，会加快速度，但是机子的负荷会加大，实测训练时本机 5060 显卡 GPU 使用跑到了 70-80%
- `x = x.to(device, non_blocking=True)` / `y = y.to(device, non_blocking=True)`
  让显存拷贝和计算重叠

经过以上三个操作，一轮的训练时间到 6-7s，训练全程 10 分钟，有显著提升。

**2. 就实际来说其对比之前的 CNN 提升依旧有限**

原本的 AlexNet 是为了 120\*120 的大批量图片设计，但是这里只用于处理少量的 28\*28 的图片，所以卷积核肯定是减小的，属于是不合使用情况了，所以提升有限。

### 2. ResNet

ResNet 最大的区别在于加入了残差连接，其意义在于对之前提取到的特征也进行了继承，这样可以避免无效特征的提取取代有效特征的情况。

所以在设计思路上，依旧以前面的代码为基础，resnet 的代码加入了残差连接块，同时在池化层的思路上十分不同：前面用卷积提取特征，stage 切换时用 stride 卷积下采样，最后用全局平均池化汇总空间信息，而不是在每个残差块后都使用最大池化。

#### 遇到的问题

**1. 为什么将前面的 x 和后面处理到的简单相加就可以得到更好的效果**

**解决：**

1. **可以保留原始信息**
   卷积层即使没有学好，也不会完全破坏原来的特征。
2. **更容易学习**
   网络不需要直接学习完整目标函数，只需要学习"在原特征上应该增加或修改什么"，即残差 F(x)。
3. **缓解梯度消失**
   shortcut 提供了一条较直接的梯度传播路径，使深层网络更容易训练。
4. **避免深层网络退化**
   如果某一层没有必要改变输入，可以让 F(x) 接近 0，此时输出近似于原输入 x。

---

## LEVEL4：语义分割 U-Net

主要分为**主干特征提取网络**和**加强特征提取网络**。

主干提取网络与 VGG 结构相似，即多个卷积层 + 最大池化层，利用这一步可以得到五个初步的有效特征层。

加强特征提取网络主要由**上采样**和**与之前得到的有效特征层拼接**这两部分构成。

经过前面两层后即进行预测，即对图片的每个像素点进行分类。

### 评价指标

- **PSNR（Peak Signal-to-Noise Ratio，峰值信噪比）：**
  用于衡量两张图像的像素级差异，通常比较"模型输出图像"和"真实图像 GT"。PSNR 越高，表示图像失真越小、越接近原图。常用于去噪、超分辨率、压缩、重建任务。
- **SSIM（Structural Similarity Index Measure，结构相似性）：**
  用于衡量两张图像在亮度、对比度和结构上的相似程度。SSIM 越接近 1，表示两张图越相似。相比 PSNR，SSIM 更接近人眼对图像质量的感知。

### 思路

#### 数据准备

1. **`scan_pairs`**
   因为该数据集分为两个文件夹中对应的图片，所以需要将输入图片与 GT 进行遍历匹配并返回输入输出路径，并且运用 `to_gray` 丢弃不需要的文件
2. **`to_gray`**
   将图片统一转灰度
3. **`letterbox`**
   统一尺寸，但是不改变实际图片尺寸，而是用白边填充，这样可以避免失真
4. **`gt_ink(gpath)`**
   计算 GT 图像中黑色内容的比例，用来判断图像是否接近全白
5. **`prepare_cache()`**
   收集并清洗数据，然后划分为训练集、验证集和测试集。这里加入的 `limit`，表示限制提取大小，为之后的小样本验证创建了条件；而后通过 `limit` 计算所需要的内存大小，再加入缓存机制，随机划分训练集、验证集、测试集然后存入缓存，返回路径，并且运用之前定义的函数对图片进行了预处理

#### 模型与训练

1. **`NoteErasureDataset`**
   创建数据集并且对其做文档增强（包括随机旋转、缩放、裁剪、透视变换），这样可以增加数据集样本多样性
2. **`DoubleConv`**
   定义模型的基本单元：两个 3\*3 的卷积 + BN 归一化 + relu 激活函数
3. **`UNet`**
   定义 4 层下采样 + 4 层上采样的 U-Net 模型。编码器每下采样一次通道翻倍，解码器每上采样一次通道减半，并把编码器对应层的特征用 `cat` 拼过来
4. **`psnr`**
   计算 psnr
5. **`ssim`**
   计算 ssim
6. **`evaluate_cache`**
   提前加载验证集，加快速度，并提前定义验证函数
7. **`train`**
   对模型进行训练并保存参数 + 绘制图像。大致结构与前面相同，但是这里加入了各种信息的打印，使流程可视化加强
8. **`plot`**
   绘制曲线
9. **`evaluate_only`**
   在测试集上先做评估
10. **`save_demos`**
    保存 4 张图片作为示例
11. **`predict`**
    单独测某一张图

### 总体思路与反思

模型的搭建与之前的模型相差不大，但是在最初半手搓的模型表现非常差，所以该代码的编写基本全部由 AI 完成。AI 在数据的导入上，加入了舍弃不需要的图片，进行多种方法的数据集的图片增强，载入缓存机制减少运行时间，包含了确立构造合适且匹配的大小结构，并且不断进行格式的转换，并且后面的模型也因此十分复杂，在读代码上我就遇到许多困难，只能大致读懂 AI 在干什么，具体怎么操作在很多地方还没有搞懂。

从保存的四张图片来看：

- Demo1 到 Demo3 表现良好，可能由于笔迹颜色有明显差异
- 而 Demo4 完全没有擦除，说明模型有待加强

### 改进思路

1. **增加数据集**
   但是由于 epochs 从 3000 升到 6000 依旧存在问题，且训练成本会急速增加，所以可行性较差，所以我认为主要还是单 U-Net 模型有局限性
2. **GAN**
   这里可以选择在 U-Net 后面加上一个 PatchGAN 模块，用于对局部特征的真假进行判断，从而优化效果
3. **扩散模型**
   这个可以通过对图片进行不断的加噪，然后再训练神经网络，这样就可以运用损失函数对模型进行预测，使得可能能够得到更好的效果

---

### 关于wsl

wsl优点在于使开发环境与数据生产环境一致，但是在本项目并不适用，反而会使速度更慢

**windowsLV1_MLP.py	6.098s	    而Linux7.649s	Windows 快**
**WindowsLV1_MLP_2.py	5.898s	而Linux7.864s	Windows 快** 
**windowLV2_CNN.py	6.813s	    而Linux8.529s	Windows 快**
**WindowsLV2_CNN_2.py	6.716s	而Linux8.730s	Windows 快**
**WindowsLV3_AlexNet.py	15.961s	而Linux15.853s	差不多**
**WindowsLV3_ResNet.py	25.802s	而Linux25.881s	差不多**